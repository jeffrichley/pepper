{
  "mcpServers": {
    "pepper-channel": {
      "command": "uv",
      "args": ["run", "--project", "{{ project_path }}", "pepper-channel"]
    },
    "pepper-discord": {
      "command": "uv",
      "args": ["run", "--project", "{{ project_path }}", "pepper-discord"]
    },
    "pepper-scheduler": {
      "command": "uv",
      "args": ["run", "--project", "{{ project_path }}", "pepper-scheduler"]
    }
  }
}
